// This Javascript file provides some of the common code and variables found in every other JS file.

"use strict"; // Not using the function form of "use strict" to ensure it applies to all files

var hostname = "mapserver.geog.uw.edu"; // Change this when changing hostname